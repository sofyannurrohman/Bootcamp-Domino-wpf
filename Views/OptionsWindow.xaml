<Window x:Class="DominoGameWPF.Views.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Options"
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

        <TextBlock Text="Audio Settings"
                   Foreground="White"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock Text="Music Volume" Foreground="White" Margin="0,0,0,5"/>

        <Slider x:Name="VolumeSlider"
                Width="200" Minimum="0" Maximum="1"
                ValueChanged="VolumeSlider_ValueChanged"
                Value="0.3"/>

        <CheckBox x:Name="MuteCheckBox"
                  Content="Mute"
                  Foreground="White"
                  Margin="0,5,0,20"
                  Checked="MuteCheckBox_Checked"
                  Unchecked="MuteCheckBox_Unchecked"/>

        <Button Content="Close"
                Width="120" Height="30"
                Background="#169976"
                Foreground="Black"
                FontWeight="Bold"
                Click="Close_Click"/>
    </StackPanel>
</Window>
